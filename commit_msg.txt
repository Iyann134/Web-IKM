feat(admin): implement full admin dashboard with CRUD for news, achievements, and board management

LAYOUT & NAVIGATION:
- Create AdminLayout.jsx as parent wrapper with collapsible sidebar state management
- Create AdminSidebar.jsx with smooth 300ms slide transition and mobile overlay backdrop
- Add active NavLink highlighting for Berita, Prestasi, and Pengurus menu items
- Add hamburger toggle button in every admin page header
- Main content area adjusts left margin dynamically based on sidebar open state

AUTHENTICATION:
- Create Login.jsx page with username/password form and inline error validation
- AdminLayout protects all admin routes by checking localStorage JWT token
- Fix logout: replace window.location.href with navigate() + reload() to fully clear React state
- Auto-redirect to /admin/login when token is missing or invalid

ADMIN BERITA PAGE:
- Responsive table with Cover, Title, Date, Content, and Action columns
- Add/Edit modal overlay with text inputs and custom photo upload dropzone
- Real-time search filter by title and content
- Edit and Delete action buttons with confirmation dialog
- Fallback to dummyBerita (6 items) when Supabase returns empty array
- Fix fallback: replace if/else with Array.isArray() for robust null/empty check

ADMIN PENCAPAIAN PAGE:
- Responsive table with Cover, Achievement Name, Event Name, and Date columns
- Add/Edit modal overlay with text inputs and custom photo upload dropzone
- Real-time search filter by achievement name
- Fallback to dummyPrestasi (6 items) when Supabase returns empty array

ADMIN PENGURUS PAGE:
- Organization chart photo upload card with image preview
- Upload and delete photo buttons with confirmation dialog
- Integrated uploadImage API with Bearer token authentication

ROUTING:
- Register all admin routes in App.jsx nested under AdminLayout
- /admin/dashboard redirects to /admin/berita
- /admin/berita -> AdminBerita, /admin/prestasi -> AdminPencapaian, /admin/pengurus -> AdminPengurus

DATA & ASSETS:
- Add dummyBerita.js with 6 news items and local image imports as fallback
- Add dummyPrestasi.js with 6 achievement items and local image imports as fallback
- Remove all absolute positioning from Figma-generated code for responsive layout
